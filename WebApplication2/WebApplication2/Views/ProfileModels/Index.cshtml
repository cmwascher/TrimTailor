@model IEnumerable<WebApplication2.Models.ProfileModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mes_waist)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mes_waist)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ApplicationUserID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ApplicationUserID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ApplicationUserID })
        </td>
    </tr>
}

</table>
